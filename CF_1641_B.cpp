
#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e5 + 10;
int n,m;
void solve() {
    cin>>n>>m;
    int ans = 0;
    for(int i = n; i >= 2 ; -- i) {
        int p = log(m)/log(i);
        
    }
}
signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int T = 1;
    cin >> T;
    while (T--) {
        solve();
    }
}